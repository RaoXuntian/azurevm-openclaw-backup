- 2026-03-24: User reminded me that for search/news requests, especially when native `web_search` hits 429/rate limits, I should prioritize the custom `no-card-search` fallback skill instead of improvising with weaker sources.
- Diagnosed why `no-card-search` was not loading: its `SKILL.md` frontmatter failed YAML parsing because the single-line `description` contained `site:example.com`. Fixed both `~/.openclaw/skills/no-card-search/SKILL.md` and `~/workspace/skills/no-card-search/SKILL.md` by changing `description` to a block scalar (`>`). After the fix, `openclaw skills info no-card-search` showed it as Ready from `openclaw-workspace`.
- During that troubleshooting I triggered a gateway restart around 2026-03-24 13:56 Asia/Shanghai; service recovered by about 14:04. User noticed and asked about it.
- Important user feedback: they feel I have become slower / "变笨了". They value shorter, more direct answers, conclusion-first responses, immediate fallback behavior on 429, and fewer heavy-handed actions like unnecessary gateway restarts.
- User looked into iMessage channel setup but decided it was too complex and not worth doing for now.
- 2026-03-25 noon: discovered `MEMORY.md` was missing from the workspace root. Searched workspace for moved/renamed copies and found none; no useful git history for recovery. Reconstructed a new `MEMORY.md` from surviving `memory/*.md` notes and session history, then restored it to `/home/xtrao/.openclaw/workspace/MEMORY.md`.
